# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
mangum==0.17.0

# PDF Processing
pdfplumber==0.10.3

# NumPy - must be installed first for binary compatibility
numpy==1.24.3

# NLP and Language Processing (depends on numpy)
spacy==3.5.3
pycountry==22.3.5

# SpaCy English model (installed as package)
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.5.0/en_core_web_sm-3.5.0-py3-none-any.whl

# HTTP Client (for potential external API calls)
httpx==0.25.0
requests==2.31.0  # For warmup script

# Additional Dependencies
python-multipart==0.0.6  # Required for file uploads in FastAPI
